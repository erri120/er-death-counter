name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7

      - name: Configure
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Package
        shell: pwsh
        run: |
          mkdir staging
          Copy-Item build/Release/death_counter.dll staging/
          Copy-Item death_counter.ini staging/
          Copy-Item fonts/OFL.txt staging/
          Compress-Archive -Path staging/* -DestinationPath death_counter.zip

      - uses: actions/upload-artifact@v7
        with:
          name: death_counter
          path: death_counter.zip
